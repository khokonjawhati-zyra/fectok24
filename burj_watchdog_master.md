# BURJ WATCHDOG: THE IMMUTABLE NEURAL GUARDIAN (MASTER BLUEPRINT)

This is the definitive guardian logic for the Sovereign V15 Ecosystem. Purpose-built to maintain the "0-Latency, 100% Green" status of all neural paths from UI to Vault.

---

## 1. The Core Purpose (ইঞ্জিনের মূল লক্ষ্য)
To ensure every **Click** from the User Panel and Admin Panel reaches its destination (Auth Vault, Media Vault, API, Dashboard) without internal bottlenecks or protocol-induced failures.

---

## 2. Neural Path Mapping (নার্ভ ট্র্যাফিক ট্র্যাকিং)
The Watchdog monitors the four critical pillars of the FecTok v15 system:

### A. The Admin Handshake Path:
`Admin UI -> Nginx Gateway (Port 80) -> API Engine (5000) -> Auth Vault (JSON) -> Handshake Success`

### B. The User Sync Path:
`User UI -> API Node -> User Data Ledger -> Profile Sync -> 0-Latency Load`

### C. The Media Stream Path:
`Playback Request -> Media Hub (R2 Mount) -> Media Vault Registry -> Video Stream End-point`

### D. The OTP & Auth Nerve:
`Login Trigger -> Backend SMTP/SMS Tunnel -> Oxygen Bridge -> Validation Success`

---

## 3. The "Hands-Off" Protocol (জগাখিচুড়ি প্রতিরোধ)
**CRITICAL RULE:** This engine is a "Passive Observer" until a **State Violation** occurs.
*   **Green Protection:** If a nerve or signal is **GREEN**, the engine is strictly forbidden from touching, re-invoking, or restarting it.
*   **Zero Interference:** It will not interfere with active user sessions, running uploads, or successful handshakes.

---

## 4. Resurrection & Path Cleaning (রাস্তা পরিষ্কার লজিক)
**Logic:** If a nerve turns **RED** (Dead Line), the watchdog acts as a "Traffic Warden":
1.  **Detection:** Identifying exactly where the pulse dropped (Nginx, API port, or Vault Lock).
2.  **Obstruction Removal:** It clears internal temporary locks or dead sockets that block the request.
3.  **Resurrection:** It re-invokes the specific dead service **without changing any code or logic**.
4.  **No Code Tampering:** It has **ZERO Write Access** to your source code or database structures. It treats the DNA as "Immutable".

---

## 5. State-Aware Intelligence (কঠোর জগাখিচুড়ি সুরক্ষা)
To avoid any "Chaos" (জগাখিচুড়ি), the engine uses a **Dual-Verification Model**:
- It must receive two consecutive "Fail" pulses before taking any restorative action.
- It logs every "Recovery Pulse" in the **Neural Ledger**, ensuring total transparency of what was fixed and why.

---

## FOUNDATION STATUS: [INITIAL IGNITION ACTIVE]
**BURJ WATCHDOG IS NOW THE PERMANENT GUARDIAN OF THE SOVEREIGN DNA.**
